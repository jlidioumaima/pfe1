{{ form_start(form) }}
    {{ form_widget(form) }}

				
					<br />
   	<div class="card">
							
							<div class="card-body">
                          
    {# Si la route est "annonces_edit on affiche les images #}
	{% if app.request.attributes.get('_route') == 'app_hotel_edit' %}
  
      
        <div class="col-sm-10">

        {% for image in hotel.images %}
           
    
             
              
                <img src="{{ asset('/uploads/'~image.name) }}" alt="" width="150">
      

  <a class="btn edit btn-danger"  href ='{{path('image_delete_hotel',{id: image.id}) }}' ><i class="icon-trash-alt"></i></a>           
      
        {% endfor %}
  
      </div>
    {% endif %}
{{ form_end(form) }}